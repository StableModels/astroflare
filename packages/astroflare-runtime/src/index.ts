/**
 * @astroflare/runtime — server render() + browser hydration.
 *
 * Phase 3 fills `render.ts`. Phase 5 fills `hmr-client.ts`. Phase 8 fills `hydration.ts`
 * (the `<astro-island>` custom element implementing `client:load|idle|visible|media`).
 */
export const RUNTIME_VERSION = "0.0.0";
