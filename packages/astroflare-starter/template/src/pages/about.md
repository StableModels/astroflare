---
title: About
---

# About this site

This page is rendered from a markdown file at `src/pages/about.md`.
The frontmatter at the top is parsed as YAML; the body becomes HTML.

Astroflare loads the same markdown rendering pipeline as Astro, with
syntax-highlighted code blocks via Shiki:

```ts
const greeting: string = "hello";
console.log(greeting);
```
