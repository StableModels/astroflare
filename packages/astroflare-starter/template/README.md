# my-astroflare-site

Bootstrapped from `@astroflare/starter`. Run `af deploy` to publish, or
embed in a host worker via `@astroflare/host-cloudflare`. Edit
`src/pages/`, drop new entries under `src/content/blog/`, and customise
the layout in `src/layouts/Base.astro`.
